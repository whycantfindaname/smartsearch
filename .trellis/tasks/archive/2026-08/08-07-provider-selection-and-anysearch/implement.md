# Implementation

1. 重构 Context7 selector 并接入两条 auto docs 路径。
2. 增加低置信度 fallback 和候选选择测试。
3. 增加 AnySearch repeatable params 与 extract local truncation。
4. 同步 CLI help、README、两份 skill 和 spec。
5. 跑 focused tests、mock smoke、skill parity 和最小 live probes。
